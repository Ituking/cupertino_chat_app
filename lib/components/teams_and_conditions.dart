import 'package:flutter/material.dart';

class TeamsAndConditions extends StatelessWidget {
  const TeamsAndConditions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
